Contains shared property sheet files for the vcxproj projects.

LcfTools:
Default compiler settings

EasyDev:
Adds EASYDEV_MSVC include/library dirs

liblcf:
Adds liblcf and dependencies (implicates EasyDev)

SDL:
Adds SDL and dependencies (implicates EasyDev)
